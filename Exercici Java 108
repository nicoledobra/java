Crea una classe Java de nom WindowLeftFL que heredi de la classe JFrame.

Aquesta classe ha de començar important els paquets necessaris per construir una GUI:

import javax.swing.*;

import java.awt.*;

import java.awt.event.*;

A continuació cal definir un constructor per aquesta classe que rebi com a paràmetre un String amb el títol per a la nova finestra que volem construir. Aquest constructor ha de cridar al constructor de la superclasse passant-li el títol i, després, ha d'establir la mida de la finestra a 75 per l'alçada i a 500 per l'amplada. També ha de definir com a operació per defecte sortir de l'aplicació en tancar la finestra (JFrame.EXIT_ON_CLOSE). El constructor també ha de crear un panell contenidor i un FlowLayout. El FlowLayout ha de tenir una aliniació a la esquerra (FlowLayout.LEFT).

Cal també crear un JButton amb el text "I am the first button" i afegir-lo al panell contenidor (amb el mètode add() del Container). A més, també s'ha de crear un JButton amb el text "I am the second button" i afegir-lo al panell contenidor (amb el mètode add() del Container).

Finalment crea una nova classe de nom Main que crei un nou objecte de la classe WindowLeftFL amb el títol "Example Window".  Per acabar cal fer visible la finestra.

code:
